
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Manage/Views/Shared/_Layout.cshtml";
}
@model IEnumerable<Comment>
<div class="content-wrapper">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Comment</h4>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Image</th>
                                <th>FullName</th>
                                <th>Position</th>
                                <th>Description</th>
                                <th colspan="2">Operations</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td class="py-1">
                                        <img width="40" src="~/assets/images/@item.Employee.ImageUrl" alt="image" />
                                    </td>
                                    <td>@item.Employee.Name @item.Employee.Surname</td>
                                    <td>@item.Employee.Position.Name</td>
                                    <td>@item.Description</td>

                                    <td><a asp-action="Delete" asp-route-id="@item.Id" asp-controller="Comment" class="btn btn-danger">Delete</a></td>
                                    <td><a asp-action="Update" asp-route-id="@item.Id" asp-controller="Comment" class="btn btn-success">Update</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>


